#ifndef {{ className | upper }}_HPP
#define {{ className | upper }}_HPP

#include <string>
#include <iostream>
#include <unistd.h>

class {{ className }}
{
	private:

	public:
		{{ className }}();
		~{{ className }}();
};

#endif
